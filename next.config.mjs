/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains:  ['localhost','th.bing.com','blob', 'img.freepik.com', 'www.shutterstock.com', 'seekvectorlogo.net'],
  },
};

export default nextConfig;
